Routes:
- /sociological renders “Government flexibility.txt” with its two headings.
- /technological renders “Optoelectronica.txt” plus equipo.jpeg and instalación.jpeg.
Text is shown immediately, then each title/paragraph briefly re-renders (scramble → final) when it scrolls into view.
Returning to “/” keeps “Hello friend” with the blinking cursor (no re-typing).